/**
 * This file contains config settings for different parts of oip-react-demo
 *
 * @param {String} url_basename - The URL "base" (localhost:port/url_basename). For example, if `url_basename` was set to "/demo", you could access the website at localhost:port/demo
 */
module.exports = {
	url_basename: ""
}